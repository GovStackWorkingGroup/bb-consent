[pytest]
bdd_features_base_dir = features/

markers =
    positive: marks positive tests (select with '-m "positive"')
    negative: marks negative tests (select with '-m "negative"')
    smoke: marks smoke tests (select with '-m "smoke"')
    unit: marks a unit test (select with '-m "unit"')
